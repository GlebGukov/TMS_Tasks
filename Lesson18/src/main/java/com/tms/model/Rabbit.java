package com.tms.model;

public class Rabbit extends Animal {
    public Rabbit() {
    }
    public Rabbit (String name, int age, int id) {
        setAnimal("Rabbit");
        setName(name);
        setAge(age);
        setId(id);
    }
}
